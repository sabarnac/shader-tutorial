import fragmentShader from "!raw-loader!./reflection-example/fragment.glsl";
import vertexShader from "!raw-loader!./reflection-example/vertex.glsl";

export const reflectionVertexShaderSource = vertexShader;
export const reflectionFragmentShaderSource = fragmentShader;
